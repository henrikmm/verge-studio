# Donor staging

Verbatim copies from ~/dev/Motiva_Challenge (read-only reference; never imported by app code).
See CLAUDE.md. Port-to-TS targets: node-graph.js + content-hash.js (M2), scale.js known_object mode (M3).
